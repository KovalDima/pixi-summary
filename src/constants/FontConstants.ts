import { Config } from "../Config";

export class FontConstants {
    public static readonly BITMAP_VCR_FONT = {
        fontName: "VCR_OSD_MONO",
        fontFamily: "VCR OSD Mono",
        fontSize: 50,
        fill: Config.colors.White,
    }
}